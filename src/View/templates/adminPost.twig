{% extends 'base.twig' %}
{% block content %}
<!-- Page Header-->
<header class="masthead" style="background-image: url('/assets/img/admin.jpg')">
    <div class="container position-relative px-4 px-lg-5">
        <div class="row gx-4 gx-lg-5 justify-content-center">
            <div class="col-md-10 col-lg-8 col-xl-7">
                <div class="site-heading">
                    <h1>
                        {{ title }}
                    </h1>
                </div>
            </div>
        </div>
    </div>
</header>

<!-- Main Content-->
<main class="mb-4">
    <div class="container px-4 px-lg-5">
        <div class="row gx-4 gx-lg-5 justify-content-center">
            <div class="col-md-10 col-lg-8 col-xl-7">

                {% set pTitle = post ? post.title : 'Titre du Post' %}
                {% set pHeader = post ? post.head : 'Chapô du Post' %}
                {% set pContent = post ? post.content : 'Contenu du Post' %}

                <form class="justify-content-center" action="/admin/post" method="post">
                    <input type="hidden" name="postId" value="{{post ? post.id : 0}}" />
                    <input class="form-control" name="title" type="text" placeholder="{{pTitle}}" value="{{post ? pTitle : ''}}"/>
                    <textarea class="form-control" name="head" placeholder="{{pHeader}}" style="height: 6rem">{{post ? pHeader : ''}}</textarea>
                    <textarea class="form-control" name="content" placeholder="{{pContent}}" style="height: 18rem">{{post ? pContent : ''}}</textarea>
                    <input type="submit" class="btn btn-primary text-uppercase" value='Enregistrer'>
                </form>
                
            </div>
        </div>
    </div>
</main>
{% endblock %}
